diff --git a/src/cli.rs b/src/cli.rs
index c3339dd..d201d73 100644
--- a/src/cli.rs
+++ b/src/cli.rs
@@ -36,6 +36,8 @@ struct Args {
     queue_penetration: f64,
     #[arg(long = "price-slippage-bps", default_value_t = 0.0)]
     price_slippage_bps: f64,
+    #[arg(long = "book-fill", default_value_t = true, action = clap::ArgAction::Set)]
+    book_fill: bool,
     #[arg(long = "output-root")]
     output_root: Option<PathBuf>,
     #[arg(long, default_value_t = false)]
@@ -78,6 +80,7 @@ pub fn run() -> Result<()> {
         trade_match_mode: args.trade_match_mode.clone(),
         queue_penetration: args.queue_penetration,
         price_slippage_bps: args.price_slippage_bps,
+        book_fill: args.book_fill,
     };
     let artifact_mode = resolve_artifact_mode(&args);
     let (persist, write_metrics, write_bundle, write_submission_log, materialize_artifacts) =
diff --git a/src/model.rs b/src/model.rs
index b2418dd..7bcdfe2 100644
--- a/src/model.rs
+++ b/src/model.rs
@@ -95,6 +95,12 @@ pub struct MatchingConfig {
     pub trade_match_mode: String,
     pub queue_penetration: f64,
     pub price_slippage_bps: f64,
+    #[serde(default = "default_book_fill")]
+    pub book_fill: bool,
+}
+
+fn default_book_fill() -> bool {
+    true
 }
 
 impl Default for MatchingConfig {
@@ -103,6 +109,7 @@ impl Default for MatchingConfig {
             trade_match_mode: "all".to_string(),
             queue_penetration: 1.0,
             price_slippage_bps: 0.0,
+            book_fill: true,
         }
     }
 }
diff --git a/src/runner.rs b/src/runner.rs
index 874a48e..99f33a6 100644
--- a/src/runner.rs
+++ b/src/runner.rs
@@ -672,53 +672,55 @@ fn match_orders_for_symbol(
             });
         }
 
-        if remaining > 0 {
-            for level in asks.iter_mut() {
-                if remaining <= 0 {
-                    break;
-                }
-                if level.price > order.price || level.volume <= 0 {
-                    continue;
-                }
-                let fill = remaining.min(level.volume);
-                let trade_price =
-                    slippage_adjusted_price(level.price, true, config.price_slippage_bps);
-                own_trades.push(Trade {
-                    symbol: symbol.to_string(),
-                    price: trade_price,
-                    quantity: fill,
-                    buyer: "SUBMISSION".to_string(),
-                    seller: String::new(),
-                    timestamp,
-                });
-                adjust_position(position, symbol, fill);
-                adjust_cash(cash_by_product, symbol, -(trade_price as f64 * fill as f64));
-                level.volume -= fill;
-                remaining -= fill;
-            }
-        } else if remaining < 0 {
-            for level in bids.iter_mut() {
-                if remaining >= 0 {
-                    break;
+        if config.book_fill {
+            if remaining > 0 {
+                for level in asks.iter_mut() {
+                    if remaining <= 0 {
+                        break;
+                    }
+                    if level.price > order.price || level.volume <= 0 {
+                        continue;
+                    }
+                    let fill = remaining.min(level.volume);
+                    let trade_price =
+                        slippage_adjusted_price(level.price, true, config.price_slippage_bps);
+                    own_trades.push(Trade {
+                        symbol: symbol.to_string(),
+                        price: trade_price,
+                        quantity: fill,
+                        buyer: "SUBMISSION".to_string(),
+                        seller: String::new(),
+                        timestamp,
+                    });
+                    adjust_position(position, symbol, fill);
+                    adjust_cash(cash_by_product, symbol, -(trade_price as f64 * fill as f64));
+                    level.volume -= fill;
+                    remaining -= fill;
                 }
-                if level.price < order.price || level.volume <= 0 {
-                    continue;
+            } else if remaining < 0 {
+                for level in bids.iter_mut() {
+                    if remaining >= 0 {
+                        break;
+                    }
+                    if level.price < order.price || level.volume <= 0 {
+                        continue;
+                    }
+                    let fill = (-remaining).min(level.volume);
+                    let trade_price =
+                        slippage_adjusted_price(level.price, false, config.price_slippage_bps);
+                    own_trades.push(Trade {
+                        symbol: symbol.to_string(),
+                        price: trade_price,
+                        quantity: fill,
+                        buyer: String::new(),
+                        seller: "SUBMISSION".to_string(),
+                        timestamp,
+                    });
+                    adjust_position(position, symbol, -fill);
+                    adjust_cash(cash_by_product, symbol, trade_price as f64 * fill as f64);
+                    level.volume -= fill;
+                    remaining += fill;
                 }
-                let fill = (-remaining).min(level.volume);
-                let trade_price =
-                    slippage_adjusted_price(level.price, false, config.price_slippage_bps);
-                own_trades.push(Trade {
-                    symbol: symbol.to_string(),
-                    price: trade_price,
-                    quantity: fill,
-                    buyer: String::new(),
-                    seller: "SUBMISSION".to_string(),
-                    timestamp,
-                });
-                adjust_position(position, symbol, -fill);
-                adjust_cash(cash_by_product, symbol, trade_price as f64 * fill as f64);
-                level.volume -= fill;
-                remaining += fill;
             }
         }
 
@@ -840,28 +842,30 @@ fn match_orders_for_symbol_raw_csv_tape(
         }
 
         if remaining > 0 {
-            for level in asks.iter_mut() {
-                if remaining <= 0 {
-                    break;
-                }
-                if level.price > order.price || level.volume <= 0 {
-                    continue;
+            if config.book_fill {
+                for level in asks.iter_mut() {
+                    if remaining <= 0 {
+                        break;
+                    }
+                    if level.price > order.price || level.volume <= 0 {
+                        continue;
+                    }
+                    let fill = remaining.min(level.volume);
+                    let trade_price =
+                        slippage_adjusted_price(level.price, true, config.price_slippage_bps);
+                    own_trades.push(Trade {
+                        symbol: symbol.to_string(),
+                        price: trade_price,
+                        quantity: fill,
+                        buyer: "SUBMISSION".to_string(),
+                        seller: String::new(),
+                        timestamp,
+                    });
+                    adjust_position(position, symbol, fill);
+                    adjust_cash(cash_by_product, symbol, -(trade_price as f64 * fill as f64));
+                    level.volume -= fill;
+                    remaining -= fill;
                 }
-                let fill = remaining.min(level.volume);
-                let trade_price =
-                    slippage_adjusted_price(level.price, true, config.price_slippage_bps);
-                own_trades.push(Trade {
-                    symbol: symbol.to_string(),
-                    price: trade_price,
-                    quantity: fill,
-                    buyer: "SUBMISSION".to_string(),
-                    seller: String::new(),
-                    timestamp,
-                });
-                adjust_position(position, symbol, fill);
-                adjust_cash(cash_by_product, symbol, -(trade_price as f64 * fill as f64));
-                level.volume -= fill;
-                remaining -= fill;
             }
             if remaining > 0 {
                 resting_bids.push(RestingSubmissionOrder {
@@ -870,28 +874,30 @@ fn match_orders_for_symbol_raw_csv_tape(
                 });
             }
         } else if remaining < 0 {
-            for level in bids.iter_mut() {
-                if remaining >= 0 {
-                    break;
-                }
-                if level.price < order.price || level.volume <= 0 {
-                    continue;
+            if config.book_fill {
+                for level in bids.iter_mut() {
+                    if remaining >= 0 {
+                        break;
+                    }
+                    if level.price < order.price || level.volume <= 0 {
+                        continue;
+                    }
+                    let fill = (-remaining).min(level.volume);
+                    let trade_price =
+                        slippage_adjusted_price(level.price, false, config.price_slippage_bps);
+                    own_trades.push(Trade {
+                        symbol: symbol.to_string(),
+                        price: trade_price,
+                        quantity: fill,
+                        buyer: String::new(),
+                        seller: "SUBMISSION".to_string(),
+                        timestamp,
+                    });
+                    adjust_position(position, symbol, -fill);
+                    adjust_cash(cash_by_product, symbol, trade_price as f64 * fill as f64);
+                    level.volume -= fill;
+                    remaining += fill;
                 }
-                let fill = (-remaining).min(level.volume);
-                let trade_price =
-                    slippage_adjusted_price(level.price, false, config.price_slippage_bps);
-                own_trades.push(Trade {
-                    symbol: symbol.to_string(),
-                    price: trade_price,
-                    quantity: fill,
-                    buyer: String::new(),
-                    seller: "SUBMISSION".to_string(),
-                    timestamp,
-                });
-                adjust_position(position, symbol, -fill);
-                adjust_cash(cash_by_product, symbol, trade_price as f64 * fill as f64);
-                level.volume -= fill;
-                remaining += fill;
             }
             if remaining < 0 {
                 resting_asks.push(RestingSubmissionOrder {
@@ -926,6 +932,7 @@ fn match_orders_for_symbol_raw_csv_tape(
             trade_timestamp,
             &trade.seller,
             config.price_slippage_bps,
+            config.queue_penetration,
         );
         sweep_synthetic_buy_into_liquidity(
             symbol,
@@ -939,6 +946,7 @@ fn match_orders_for_symbol_raw_csv_tape(
             trade_timestamp,
             &trade.buyer,
             config.price_slippage_bps,
+            config.queue_penetration,
         );
 
         let residual_public_quantity = synthetic_bid_remaining.min(synthetic_ask_remaining);
@@ -981,6 +989,7 @@ fn sweep_synthetic_sell_into_liquidity(
     timestamp: i64,
     counterparty_seller: &str,
     slippage_bps: f64,
+    queue_penetration: f64,
 ) {
     while *synthetic_remaining > 0 {
         let Some(source) = best_bid_liquidity_source(bids, resting_bids, trade_price) else {
@@ -993,25 +1002,28 @@ fn sweep_synthetic_sell_into_liquidity(
                 *synthetic_remaining -= fill;
             }
             RestingBidSource::Submission(index) => {
-                let fill = (*synthetic_remaining).min(resting_bids[index].quantity);
-                let execution_price =
-                    slippage_adjusted_price(resting_bids[index].price, true, slippage_bps);
-                own_trades.push(Trade {
-                    symbol: symbol.to_string(),
-                    price: execution_price,
-                    quantity: fill,
-                    buyer: "SUBMISSION".to_string(),
-                    seller: counterparty_seller.to_string(),
-                    timestamp,
-                });
-                adjust_position(position, symbol, fill);
-                adjust_cash(
-                    cash_by_product,
-                    symbol,
-                    -(execution_price as f64 * fill as f64),
-                );
-                resting_bids[index].quantity -= fill;
-                *synthetic_remaining -= fill;
+                let max_fill = (*synthetic_remaining).min(resting_bids[index].quantity);
+                let our_fill = python_round_to_i64(max_fill as f64 * queue_penetration.max(0.0));
+                if our_fill > 0 {
+                    let execution_price =
+                        slippage_adjusted_price(resting_bids[index].price, true, slippage_bps);
+                    own_trades.push(Trade {
+                        symbol: symbol.to_string(),
+                        price: execution_price,
+                        quantity: our_fill,
+                        buyer: "SUBMISSION".to_string(),
+                        seller: counterparty_seller.to_string(),
+                        timestamp,
+                    });
+                    adjust_position(position, symbol, our_fill);
+                    adjust_cash(
+                        cash_by_product,
+                        symbol,
+                        -(execution_price as f64 * our_fill as f64),
+                    );
+                }
+                resting_bids[index].quantity -= max_fill;
+                *synthetic_remaining -= max_fill;
             }
         }
     }
@@ -1029,6 +1041,7 @@ fn sweep_synthetic_buy_into_liquidity(
     timestamp: i64,
     counterparty_buyer: &str,
     slippage_bps: f64,
+    queue_penetration: f64,
 ) {
     while *synthetic_remaining > 0 {
         let Some(source) = best_ask_liquidity_source(asks, resting_asks, trade_price) else {
@@ -1041,21 +1054,28 @@ fn sweep_synthetic_buy_into_liquidity(
                 *synthetic_remaining -= fill;
             }
             RestingAskSource::Submission(index) => {
-                let fill = (*synthetic_remaining).min(resting_asks[index].quantity);
-                let execution_price =
-                    slippage_adjusted_price(resting_asks[index].price, false, slippage_bps);
-                own_trades.push(Trade {
-                    symbol: symbol.to_string(),
-                    price: execution_price,
-                    quantity: fill,
-                    buyer: counterparty_buyer.to_string(),
-                    seller: "SUBMISSION".to_string(),
-                    timestamp,
-                });
-                adjust_position(position, symbol, -fill);
-                adjust_cash(cash_by_product, symbol, execution_price as f64 * fill as f64);
-                resting_asks[index].quantity -= fill;
-                *synthetic_remaining -= fill;
+                let max_fill = (*synthetic_remaining).min(resting_asks[index].quantity);
+                let our_fill = python_round_to_i64(max_fill as f64 * queue_penetration.max(0.0));
+                if our_fill > 0 {
+                    let execution_price =
+                        slippage_adjusted_price(resting_asks[index].price, false, slippage_bps);
+                    own_trades.push(Trade {
+                        symbol: symbol.to_string(),
+                        price: execution_price,
+                        quantity: our_fill,
+                        buyer: counterparty_buyer.to_string(),
+                        seller: "SUBMISSION".to_string(),
+                        timestamp,
+                    });
+                    adjust_position(position, symbol, -our_fill);
+                    adjust_cash(
+                        cash_by_product,
+                        symbol,
+                        execution_price as f64 * our_fill as f64,
+                    );
+                }
+                resting_asks[index].quantity -= max_fill;
+                *synthetic_remaining -= max_fill;
             }
         }
     }
